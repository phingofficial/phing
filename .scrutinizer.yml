tools:
  external_code_coverage:
    timeout: 1200

filter:
  paths: ["classes/*", "test/classes/*"]
  dependency_paths:
    - "vendor/"

checks:
  php:
    duplication: false

build:
  nodes:
    analysis:
      tests:
        override:
          - 'php-scrutinizer-run --enable-security-analysis'
